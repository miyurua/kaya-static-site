import styled from 'styled-components';

export const HeaderWrapper = styled.div`
    .center-align {
        text-align: center;
    }
    .image-container {
        display: flex;
        justify-content: center;
    }
`;
